% ============================================================
%  Workshop Poster Set4MOST
% ============================================================

\documentclass[final]{beamer}

\usepackage[
  orientation=portrait,
  size=a0,
  scale=1.35
]{beamerposter}
 
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
usepackage{lmodern}
\usepackage[french]{babel}
\usepackage{graphicx}
\usepackage{amsmath}
\usepackage{xcolor}
\usepackage{tcolorbox}
\usepackage{booktabs}

% ── Bibliography ────────────────────────────────────────────────
\usepackage[backend=biber, style=numeric]{biblatex}
\addbibresource{bibliography.bib}
\AtBeginBibliography{\small}

% ── Colors ────────────────────────────────────────────────
\definecolor{Rouge}    {HTML}{C0272D}
\definecolor{RougeF}   {HTML}{8B0000}
\definecolor{GrisClair}{HTML}{F5F5F5}
\definecolor{Texte}    {HTML}{1A1A1A}

% ── Theme beamer ────────────────────────────────────────────
\usetheme{default}
\setbeamertemplate{navigation symbols}{}
\setbeamercolor{background canvas}{bg=white}
\setbeamercolor{block title}{fg=white,  bg=Rouge}
\setbeamercolor{block body} {fg=Texte,  bg=GrisClair}
\setbeamerfont{block title}{size=\large, series=\bfseries}
\setbeamerfont{block body} {size=\normalsize}

% ── Research Tracks Box ──────────────────────────────────────
\newcommand{\keybox}[1]{%
  \begin{tcolorbox}[
    colback=Rouge!10, colframe=Rouge,
    arc=3pt, boxrule=1.5pt,
    left=8pt, right=8pt, top=5pt, bottom=5pt
  ]
  \centering\large\bfseries #1
  \end{tcolorbox}}

% ============================================================
\begin{document}
\begin{frame}[t]{}

% ── Title head ────────────────────────────────────────────
\begin{columns}[c]

  \begin{column}{0.075\linewidth}
    \centering
    \includegraphics[width=1\linewidth]{images/logoCEA.png}
  \end{column}

  \begin{column}{0.075\linewidth}
    \centering
    \includegraphics[width=1\linewidth]{images/logoLARIS.png}
  \end{column}

  \begin{column}{0.5\linewidth}
    \centering
    {\color{Rouge}\bfseries\Huge
	Navigation in unknown outdoor off-road envirronement using Interval Analysis}\\[0.6cm]
    {\large
      Celian \textsc{Daligault}$^1$$^2$,
      Eric \textsc{Lucet}$^2$,
      Sebastien \textsc{Lahaye}$^1$},
	  Sebastien \textsc{Lagrange}$^1$,
	  Remy \textsc{Guyonneau}$^1$\\[0.35cm]
    {\normalsize\color{gray}
      $^1$LARIS, Universite Angers — 62 avenue Notre-Dame-du-Lac 49000 Angers France\quad
	  \newline
      $^2$nano-innov, CEA — 2 Boulevard Thomas Gobert 91120 PALAISEAU France}\\[0.2cm]
  \end{column}

  \begin{column}{0.15\linewidth}
    \centering
    \includegraphics[width=0.7\linewidth]{images/logoSet4MOST.png}
  \end{column}

\end{columns}

% ── Red separators heads ──────────────────────────────
\vspace{0.5cm}
\noindent\textcolor{Rouge}{\rule{\linewidth}{6pt}}\\[-2pt]
\noindent\textcolor{RougeF}{\rule{\linewidth}{2pt}}
% \vspace{0.1cm}


\begin{columns}[T]

  % ── Left column ───────────────────────────────
  \begin{column}{0.57\linewidth}

    \begin{block}{Context}
	\textbf{Outdoor unknown off-road navigations stakes in mobile robotics application are multiples:}

  \vspace{0.5cm}

  \begin{itemize}
    \item 	Firstly, the unkown nature of the enviroonmeent necessitate to preform a full simulatuaous localisation and mapping (SLAM) whitout or with the least possible a priori hypothesis which is a well known and classical problem.
    \item 	The off-road specifity introduces many additional important information neccessary to know such as ground geometry, its nature (e.g. slipping ground) but also the presence and nature of dynamic objects to adjust the cartography models and potentially to avoid it when path generation is produced.
    \item In addition, when a robot evolve outdoor, it face evolutionarry envirronement condition like a change in the luminosity and wheater conditions. This change in the envirronement does not always allow to use continusously the same sensors. A choice in the sensors but also multisensor fusion is consequently essential.
    \item Moreover, the noisy nature of aquistion sensors must be a the subject of careful consideration to bring robust information from which path generation can be based on. The context of the described envirronement can easely correspond to applications such as construction or farming sites when vehicules evolve jointly with human operators, application when the security is the top priority. This last constraint justify how the use of the interval analysis should be considered.
  \end{itemize}
  \end{block}

    \vspace{0.5cm}

    	\begin{block}{A robust special sensor: The micro-wave RADAR}

      \vspace{0.8em}
      \begin{center}
        \includegraphics[width=0.92\linewidth]{images/Generalshema}\\[4pt]
        {\small\itshape Figure 1. Overview of the radar integration in SLAM.}
      \end{center}
		
    \vspace{0.5cm}

    Similarly to the LiDAR, the RADAR is an electromagnetic based active sensor, which return the distance to an obstacle with the time of flight between signal transmission and reception. The difference is is that the wave frequency are in the Radio frequency area, between 3 MHz and 300 GHz (76 - 81 GHz for automotive RADAR) whereas LiDAR wave frequency is in the infrared area, aproximately 10 000 times higher. This difference in wave frequency translates in sensors property where RADAR:

    \vspace{0.5cm}

    \begin{itemize}
      \item Is less senssible to signal attenuation in the athomsphere, allowing higher maximum range and robustness to wheather conditions such as rain or fog.
      \item Return Doppler velocity of obsctacles.
      \item Can possibily go through matter depending on the type of RADAR
      \item Is cheaper than LiDAR, so attractive for industrial applications
    \end{itemize}

    \vspace{0.5cm}

    However, the RaDAR drawback also include disadvanatges compared to the radar, in fact, it is:

    \vspace{0.5cm}
    
    \begin{itemize}
      \item The accuracy of detections is poor compared to the LiDAR necessiting deeper noise consideration.
      \item The spatial resolution is weak which makes it complicated to detect small detecttion for weak Radar Cross Section target (RCS) depending on shape, geometry and material.
    \end{itemize}

    \vspace{0.5cm}

    So if the RADAR appears to be an cheap and robust sensors for hostile envirronement conditions it is complicated to use it whitout signal processing and/or whitout joining other sensors. ~\cite{radarsurvey}
    \end{block}

		% One of the main issue of range based sensor is that they are not capable to see behind a target which makes it comlicated to estimate the geneal pose whitout making assumptions on its shape nature. However because the dynamic object or the measrement plateform move multiple face of the object are visible and the accumalation of this information could at the end give a conclusion on the form of the object. The velocity tracking appears then to be a true stake.

  \end{column}


  % ── right column ───────────────────────────────
  \begin{column}{0.37\linewidth}

	\begin{block}{An application of the radar benefit: The dynamic tracking}
      {
        Dynamic objects envirronement need to be identified to 

        \vspace{0.5cm}

        \begin{itemize}
          \item Remap correctly the map that have changed.
          \item To avoid the dynamic obstacles.
        \end{itemize} 

        \vspace{0.5cm}
        
        The dynamic obsctacles are of two types:

        \vspace{0.5cm}

        \begin{itemize}
          \item Semi-static where the object does not change its position often (e.g. a door).
          \item Highly dynamic where the object often move (e.g. a person).
        \end{itemize}

        \vspace{0.5cm}

        Classically, when range-azimuth sensors are used, dynamic object tracking is performed by comparaison of multiple frame to be able to detect semi-static and high dynamicity.~\cite{radartracking}
      }
    \end{block}

	\vspace{0.5cm}

	\begin{center}
		\includegraphics[width=0.92\linewidth]{images/ICRretrieval.png}\\[4pt]
		{\small\itshape Figure 2. ICR and angular velocity retrieval with distribution of Doppler radial velocity.}
	\end{center}

  % \begin{equation}
  %    y_r \cos(\theta_i) - x_r \sin(\theta_i) = y_{ICR} \cos(\theta_i) - x_{ICR} \sin(\theta_i) - v_{d_i} \cdot \omega^{-1}
  % \end{equation}

	\begin{center}
		\includegraphics[width=0.92\linewidth]{images/projection.png}\\[4pt]
		{\small\itshape Figure 3. Current measured point projection with ICR and angular velocity estimation.}
	\end{center}

	\vspace{0.5cm}

    % Research Tracks
    \begin{tcolorbox}[
      title={\large\bfseries\color{white} Research tracks},
      colbacktitle=Rouge,
      colback=Rouge!12,
      colframe=Rouge,
      arc=3pt, boxrule=1pt,
      left=8pt, right=8pt, top=6pt, bottom=6pt
    ]

    \begin{itemize}
      \item To use the Doppler Velocity of the Radar to map the turn of the object.
      \item To use  the shape estimation of the object to obtain conclusion on static map and dynamic obstacles uncertainities.
    \end{itemize}
    \end{tcolorbox}

    \vspace{0.5cm}
    
    \begin{block}{Bibliography}
      {\fontsize{7}{9}\selectfont
        \printbibliography
      }
    \end{block}

    \vspace{0.5cm}

  \end{column}

\end{columns}

% ── Low Red  ──────────────────────────────────────
\vspace{0.7cm}
\noindent\textcolor{RougeF}{\rule{\linewidth}{2pt}}\\[-2pt]
\noindent\textcolor{Rouge}{\rule{\linewidth}{6pt}}

% ── Botom text  ────────────────────────────────────────────

\vspace{0.5cm}
\noindent\textcolor{RougeF}{\rule{\linewidth}{2pt}}\\[-2pt]
\noindent\colorbox{Rouge}{%
  \begin{minipage}{\linewidth}
    \vspace{0.3cm}
    \centering
    {\color{white}\large\bfseries Set4MOST · Oldenburg · 20/04/2026 - 24/04/2026 - \normalsize celian.daligault@cea.fr}\\[0.2cm]
    \vspace{0.2cm}
  \end{minipage}%
}


\end{frame}
\end{document}